import debug from 'debug';

export default class Logger {
    /* --- globals --- */

    /* --- constants --- */

    /* --- constructor --- */

    /**
     * Initializes the logger
     *
     * @param  [String] id
     */
    constructor(id) {
        return debug(id);
    }

    /* --- protected --- */

    /* --- public --- */
}
